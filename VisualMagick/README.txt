Before you can build the ImageMagick system, you must first build and run the
configure utility. You will find it in the "configure" subdirectory inside this
directory. Open its workspace "configure.dsw" and build it (hit F7), then just
execute the program (Ctrl-F5).